[project]
name = "w-seminar"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.10.7",
    "scipy>=1.16.3",
]

[tool.uv.scripts]
planck = "echo test"